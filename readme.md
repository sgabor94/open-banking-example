### Testing application

Start the application with the default config:

Just start without any environment variables to read the default config from the config folder.

OR start with custom config:

Environment variables:

- `SPRING_CONFIG_IMPORT=file:<path_to_config_file>`
